import 'package:flutter/material.dart';
import 'package:qcplayer/constants/common.dart';

class AppModel {
  Locale locale = Locale(Common.defaultLanguageCode, Common.defaultCountryCode);

  ThemeMode appThemeMode = Common.defaultAppThemeMode;
}